import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

export default function HealthTimeline({ intervals = [] }) {
  const data = intervals.map((days, index) => ({ sequence: index + 1, days }));
  if (!data.length) return <p className="py-16 text-center text-sm text-text-secondary">At least two dated records are required to calculate failure intervals.</p>;
  return <div className="h-52 w-full" role="img" aria-label="Observed days between recorded failures"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 8, right: 8, left: -12, bottom: 0 }}><defs><linearGradient id="intervalFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#a96300" stopOpacity={0.35} /><stop offset="100%" stopColor="#a96300" stopOpacity={0.03} /></linearGradient></defs><CartesianGrid stroke="#d9e0e7" strokeDasharray="3 3" vertical={false} /><XAxis dataKey="sequence" stroke="#667489" tickLine={false} axisLine={false} tick={{ fontSize: 11 }} label={{ value: "Interval sequence", position: "insideBottom", offset: -2 }} /><YAxis stroke="#667489" tickLine={false} axisLine={false} tick={{ fontSize: 11 }} /><Tooltip contentStyle={{ background: "#ffffff", border: "1px solid #d9e0e7", borderRadius: 6, color: "#172033" }} formatter={(value) => [`${value} days`, "Observed interval"]} /><Area type="monotone" dataKey="days" stroke="#a96300" strokeWidth={2} fill="url(#intervalFill)" /></AreaChart></ResponsiveContainer></div>;
}