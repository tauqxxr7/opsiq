import { UploadCloud } from "lucide-react";

export default function UploadZone({ onFiles, busy }) {
  return <label className={`flex min-h-48 flex-col items-center justify-center rounded-lg border border-dashed px-6 text-center transition-colors ${busy ? "cursor-wait border-border bg-card" : "cursor-pointer border-muted/50 bg-surface hover:border-primary hover:bg-primary/[0.03]"}`}><UploadCloud className="text-primary" size={28} /><strong className="mt-4 text-sm">{busy ? "Processing document..." : "Upload operational evidence"}</strong><span className="mt-1 text-xs text-text-secondary">PDF or DOCX · backend limits apply · duplicate content is rejected</span><input type="file" accept=".pdf,.docx" className="sr-only" disabled={busy} onChange={(event) => onFiles(event.target.files)} /></label>;
}