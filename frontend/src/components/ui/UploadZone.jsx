
import {UploadCloud} from "lucide-react";export default function UploadZone({onFiles,busy}){return <label className="flex min-h-52 cursor-pointer flex-col items-center justify-center rounded-xl border border-dashed border-border bg-card/30 text-center hover:border-primary"><UploadCloud className="text-primary" size={32}/><strong className="mt-4 text-sm">{busy?"Processing document…":"Drop PDF or DOCX files here"}</strong><span className="mt-1 text-xs text-text-secondary">or click to browse · secure local processing</span><input type="file" accept=".pdf,.docx" className="hidden" disabled={busy} onChange={e=>onFiles(e.target.files)}/></label>}
