
import {useState} from "react";import {Bot,Send,FileText,User} from "lucide-react";import {ask} from "../services/api";import CitationChip from "../components/ui/CitationChip";import ConfidenceBar from "../components/ui/ConfidenceBar";export default function ExpertCopilot(){const[messages,setMessages]=useState([{role:"assistant",content:"Ask me about indexed procedures, safety standards, equipment history or work orders. I will answer only from retrieved evidence.",citations:[],confidence:null}]);const[input,setInput]=useState("");const[loading,setLoading]=useState(false);const[active,setActive]=useState(null);const send=async()=>{if(!input.trim()||loading)return;const q=input;setInput("");setMessages(x=>[...x,{role:"user",content:q}]);setLoading(true);try{const data=await ask({query:q});setMessages(x=>[...x,{role:"assistant",content:data.answer,citations:data.citations||[],confidence:data.confidence}])}catch{setMessages(x=>[...x,{role:"assistant",content:"The knowledge service did not respond within 30 seconds. Verify backend connectivity and try again.",citations:[],confidence:0}])}finally{setLoading(false)}};return <div className="grid min-h-[calc(100vh-8rem)] gap-5 xl:grid-cols-[1fr_320px]"><section className="flex min-h-[650px] flex-col overflow-hidden rounded-xl border border-border bg-surface"><header className="border-b border-border p-5"><h1 className="font-semibold">Expert Knowledge Copilot</h1><p className="text-xs text-text-secondary">Hybrid RAG · cross-encoder reranking · citation-enforced</p></header><div className="flex-1 space-y-5 overflow-y-auto p-5">{messages.map((m,i)=><div key={i} className={"flex gap-3 "+(m.role==="user"?"justify-end":"")}><div className="mt-1 hidden h-8 w-8 items-center justify-center rounded-lg border border-border bg-card sm:flex">{m.role==="user"?<User size={15}/>:<Bot size={15}/>}</div><div className="max-w-2xl"><div className={"rounded-xl border px-4 py-3 text-sm leading-6 "+(m.role==="user"?"border-primary/30 bg-primary/15":"border-border bg-card")}>{m.content}</div>{m.confidence!=null&&<div className="mt-3"><ConfidenceBar score={m.confidence}/></div>}{m.citations?.map((c,j)=><CitationChip key={j} citation={c} index={j+1} onClick={()=>setActive(c)}/>)}</div></div>)}{loading&&<div className="text-sm text-primary">Retrieving and verifying evidence…</div>}</div><div className="border-t border-border p-4"><div className="flex gap-3"><input className="min-w-0 flex-1 rounded-lg border border-border bg-card px-4 text-sm outline-none focus:border-primary" value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Ask an evidence-grounded operational question…"/><button onClick={send} disabled={loading||!input.trim()} className="rounded-lg bg-primary p-3 disabled:opacity-40"><Send size={18}/></button></div><p className="mt-2 text-xs text-muted">No evidence means no generated answer.</p></div></section><aside className="rounded-xl border border-border bg-surface p-5"><h2 className="font-semibold">Document context</h2>{active?<div className="mt-6 space-y-4"><FileText className="text-primary"/><p className="text-sm font-medium">{active.doc_name}</p><p className="font-mono text-xs text-text-secondary">Page {active.page} · {active.section}</p><p className="rounded-lg border border-border bg-card p-3 text-xs leading-5">{active.excerpt}</p><ConfidenceBar score={active.relevance_score}/></div>:<p className="mt-6 text-sm text-text-secondary">Select a citation to inspect its exact source passage.</p>}</aside></div>}
