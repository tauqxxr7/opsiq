
import {useEffect,useState} from "react";import {patterns} from "../services/api";export default function FailurePatterns(){const[data,setData]=useState(null);useEffect(()=>{patterns().then(setData).catch(()=>{})},[]);const items=data?.patterns||[{equipment_id:"P-201",failure_type:"Bearing / seal degradation",occurrences:4,risk:"CRITICAL"},{equipment_id:"P-207",failure_type:"Cavitation",occurrences:3,risk:"WATCH"}];return <div className="space-y-6"><div><h1 className="text-2xl font-semibold">Failure Pattern Engine</h1><p className="text-sm text-text-secondary">Recurring subgraphs surfaced across equipment, conditions and root causes.</p></div><div className="grid gap-5 lg:grid-cols-2">{items.map((x,i)=><article className="rounded-xl border border-border bg-surface p-5" key={i}><div className="flex justify-between"><div><p className="font-mono text-primary">{x.equipment_id}</p><h2 className="mt-2 font-semibold">{x.failure_type}</h2></div><span className={"text-xs "+(x.risk==="CRITICAL"?"text-critical":"text-warning")}>{x.risk}</span></div><div className="mt-6 flex items-center gap-3"><div className="h-2 flex-1 rounded bg-border"><div className="h-full rounded bg-critical" style={{width:Math.min(100,x.occurrences*20)+"%"}}/></div><span className="font-mono text-xs">{x.occurrences} events</span></div><p className="mt-4 text-xs leading-5 text-text-secondary">Shared precursor path detected: alignment drift → bearing temperature rise → seal damage.</p></article>)}</div></div>}
