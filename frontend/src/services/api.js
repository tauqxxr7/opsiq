
import axios from "axios";const client=axios.create({baseURL:import.meta.env.VITE_API_URL||"http://localhost:8000/api",timeout:30000});export const ask=(payload)=>client.post("/query",payload).then(r=>r.data);export const upload=(file,onProgress)=>{const body=new FormData();body.append("file",file);return client.post("/documents/upload",body,{onUploadProgress:onProgress}).then(r=>r.data)};export const maintenance=(id)=>client.get("/maintenance/"+id).then(r=>r.data);export const compliance=(id)=>client.get("/compliance/audit/"+id).then(r=>r.data);export const patterns=()=>client.get("/patterns").then(r=>r.data);export default client;
